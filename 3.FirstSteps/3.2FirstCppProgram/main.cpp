#include <iostream>

int main(){
    std::cout << "Hello from Main!" << std::endl;

    return 0;
}

// the iostream package is a builtin package used to write to terminal
// Main function is the entrypoint for c++ code, when you run the program this is what is run first
// All main functions in c++ return something (usually 0) at end. If not explicitly then compiler adds it for you.  
    // This is used as a signature for operating system to know your program finished successfully